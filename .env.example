# Copy this file to .env for local development.
# Never commit .env or put production secrets in source control.
DB_URI=mongodb://127.0.0.1:27017/message
JWT_SECRET_KEY=replace-with-a-long-random-secret
PORT=3000
